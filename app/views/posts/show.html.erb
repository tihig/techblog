<p id="notice"><%= notice %></p>

<h1>
  <%= @post.title %>
</h1>



<p>
  <%= @post.category.name %>
</p>

<p>
  <%= @post.body %>
</p>



<div class="btn-toolbar" role="toolbar" aria-label="...">
  <div class="btn-group" role="group" aria-label="...">
   <% if current_user %>
<!-- favorites_path(@post), method: :post  favorites_path(@post), method: :delete  -->
    <%= link_to "favorite", '#' , class:"btn btn-default" %>
    <% if current_user.in? @post.favorited_by %>
     <%= link_to "unfavorite", '#', class:"btn btn-default" %>
    <% end %>
  <% end %>
    <%= edit_and_destroy_buttons(@post) %>
  </div>
</div>
