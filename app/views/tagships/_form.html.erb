<%= form_for(@tagship) do |f| %>
  <% if @tagship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tagship.errors.count, "error") %> prohibited this tagship from being saved:</h2>

      <ul>
      <% @tagship.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :post_id %><br>
    <%= f.number_field :post_id %>
  </div>
  <div class="field">
    <%= f.label :tag_id %><br>
    <%= f.number_field :tag_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
